# Открытые вопросы

- Нужны ли автоматические скрипты проверки ссылок, якорей и потери текста. Пока папка `/scripts` не создана.
- Нужно ли позже выделить исходный код генератора сайта в отдельную папку, если `/site` станет трудно поддерживать как артефакт.
- Нужно ли создавать вложенные `AGENTS.md`, если Codex начнёт путать режимы работы. Текущее решение — не создавать.
- Какие материалы из `/work/` после завершения рабочей ветки должны быть повышены до постоянных `/project`, `/protocols`, `/site-spec` или `/content`, а какие должны остаться только в истории ветки.
- Нужно ли формализовать decision records отдельными файлами, если `project/decisions.md` станет слишком большим.

- Нужно ли позже вынести отдельные skills из `protocols/skills/*.md` в папки с `SKILL.md`, если Codex потребует нативный формат или если skills обрастут шаблонами/скриптами.
