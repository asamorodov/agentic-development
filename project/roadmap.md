# Ближайший план

1. Создать чистый `main` без `/work` и проверить постоянную структуру.
2. Дать Codex первый запрос из `protocols/prompts/first-codex-task.md` без права на правки.
3. Проверить, как Codex понимает роли `/content`, `/site`, `/site-spec`, `/protocols`, `/project` и `/work`.
4. Выполнить малую правку сайта или проверочный аудит сайта.
5. После этого создать ветку `content/theory-rebuild-2026-06` с `/work` и рабочей зоне `/work/`; первой точкой входа для этой работы считать `work/discourse.md`.
6. Интегрировать принятые результаты перестройки теории в `/content/Theoretical_synthesis.md` только после human gate.
