# VERIFY — C5_THEORY_TO_TECHNICAL_ATLAS

Статус: `completed`  
Готовность: `ready_with_known_debts / A10 sync pending`

## Контрольная проверка

| Проверка | Итог |
| --- | --- |
| Основной C5-фрагмент существует | Да: `work/theory-writing/fragments/C5_theory_to_technical_atlas.md`. |
| Семь обязательных C5-файлов присутствуют | Да: основной фрагмент, source usage, story anchor map, article registry, figure candidates, open questions, degradation/duplication audit. |
| Источники и provenance отделены | Да: `C5_source_usage.md` фиксирует внутренние входы, внешние первичные источники и маршрутизацию фактов между теорией, статьёй, источниковой заметкой, исторической заметкой, Handbook и Fieldbook. |
| Истории не пересказаны вместо теории | Да: `C5_story_anchor_map.md` оставляет истории якорями и маршрутами чтения, а не полноценными нарративами в основном C5. |
| Карта статей не стала мини-атласом | Да: `C5_concept_atlas_article_map.md` держит уровень реестра — вопрос читателя, границу, допустимый повтор, готовность источников/ассетов и семантические обратные связи. |
| Визуальные решения классифицированы | Да: `C5_figure_candidates.md` различает `external_real_image_candidate`, синтетические фигуры и запреты. |
| Реальные изображения не заменены текстовыми суррогатами | Да: реальные диаграммы, скриншоты и фигуры оставлены кандидатами до отдельного asset/source-прохода. |
| Синтетические фигуры проходят смысловой фильтр | Да: они оставлены только как кандидаты, которым нужна проверка полезности, нетривиальности и недублирования текста. |
| A10 отсутствует без блокировки C5 | Да: статус оформлен как `A10 sync pending`, а не как причина не закрывать C5. |
| Открытые вопросы отделены | Да: долги вынесены в `C5_open_questions.md`. |
| Аудит деградации и дублирования есть | Да: `C5_degradation_and_duplication_audit.md` содержит регрессионные проверки и статус готовности. |
| Служебный мета-текст не оставлен в основном фрагменте | Да: проверка по основному C5 не нашла внутренних названий рабочих листов, скриптовых маркеров или служебных проходов. |
| URL не испорчены языковой правкой | Да: проверка не нашла кириллицы внутри URL. |
| HTML/figure-блоки не сломаны | Да: новые `figure`-блоки не вставлялись; открытых `figure`/`/figure` в C5-файлах нет. |

## Машинная проверка упаковки

Проверено перед упаковкой:

- все семь обязательных фрагментов существуют;
- во всех URL отсутствуют кириллические символы;
- в рабочих C5-файлах нет открытых HTML-figure-блоков;
- архив содержит ровно требуемые C5-файлы и три финальных служебных файла.

## Известные долги, не блокирующие упаковку

- `A10 sync pending`: будущая синхронизация с A10 после появления/готовности A10.
- Отдельная свежая проверка быстро меняющихся инструментов перед написанием каждой статьи атласа.
- Отдельный проход по правам, качеству и локализации реальных визуальных ассетов.
- Самостоятельный аудит дублирования для каждой будущей статьи атласа.
