# Chapter V — wording patch for Constitutional SDD

## Summary

The wording around `Constitutional SDD` was cleaned up after review. The phrase `конституционный подход` sounded like an artificial political metaphor in Russian and made the text less technical.

The chapter now keeps `Constitutional SDD` as the name of the method/source line, but ordinary prose uses more direct wording:

- `подход через правила проекта верхнего уровня`;
- `правила проекта верхнего уровня`;
- `набор правил проекта`;
- `правила выше локальной задачи`.

## Main changes

- Replaced `конституционный подход` with `подход через правила проекта верхнего уровня`.
- Replaced `конституционные правила` with `правила проекта верхнего уровня`.
- Replaced general uses of `конституция проекта` with `набор правил проекта` or `правила проекта верхнего уровня`.
- Added a clarification that `constitution` is part of the English method vocabulary here, not a political metaphor.
- Kept file and method names such as `Constitutional SDD`, `constitution.md`, and `banking-ms-by-constitution` unchanged.

## Validation

- No Cyrillic `конституц*` forms remain in the main chapter text.
- `Constitutional SDD` remains as the source/method name.
- The chapter still compares four ways of working with specifications: Spec Kit, Kiro Specs, TDAD, and Constitutional SDD.
