# Open questions — Chapter VII

1. How much English terminology should remain in headings? `Claim`, `Gate`, `Source state`, `Prime` are useful exact terms, but final Russian prose should not feel half-translated.
2. Should `fig-vii-summary-vs-work-graph` and `fig-vii-signal-triage` remain HTML tables, or should site CSS use card layout for narrow screens?
3. Should chapter introduce a synthetic state-contract figure, or is the Beads image plus two tables enough?
4. How much of Beads coordination should remain in main text? Current structural plan uses only pin/hook/handoff/reservation as examples of ownership semantics.
5. Should `source state` be translated more consistently as «состояние источника» in prose while keeping `source state` in code/field examples?
6. Is `restoration packet` acceptable as term, or should chapter use «пакет восстановления» as primary and leave English in parentheses only at first mention?
7. Are runtime/worktree boundary paragraphs still too close to chapter IX? If final chapter feels long, compress them.
