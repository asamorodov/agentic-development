# VIII — open questions

Статус: заполнено по текущему тексту главы.

1. **Вставлять ли synthetic figure?** Основной кандидат `fig-viii-process-profile-interface` почти наверняка полезен. Решение зависит от будущего формата сборки теоретических глав.

2. **Нужна ли вторая схема по `billing/API`?** Таблица уже работает. Схема нужна только если финальный visual pass решит разгрузить таблицу.

3. **Не слишком ли плотен BMAD-раздел?** После P14 он возвращён к функции anchor-method, но остаётся самым длинным. Будущие доборы по BMAD должны не расширять главу, а заменять менее важные детали.

4. **Сколько source-native terms оставить?** В тексте остаются `story`, `workflow`, `execution`, `investigation`, `review`, `checkpoint`, `brownfield`, `case file`, `gates`, `Confirmed/Deduced/Hypothesized`. Финальный pass должен проверить, какие из них нужны как названия, а какие можно заменить.

5. **Нужно ли возвращать Mae/Shopify?** Текущая версия не использует их, потому что Jesse/HumanLayer/Matt достаточно показывают малые профили. Возвращать только если нужен новый механизм, а не дополнительный пример.

6. **Проверить актуальность BMAD/GSD перед публикацией.** Документация меняется; если публикация будет позже, открыть текущие страницы ещё раз.
