# Open questions — Chapter IV SPDD specification lifecycle

## Closed for current package

- Нужен ли широкий внешний добор? Нет: источники достаточны для IV.
- Нужно ли дальше расширять пример биллинга? Нет: в тексте уже есть самостоятельная линия через Canvas, генерацию, проверку, ревью, `prompt-update` и `sync`; дальнейшее расширение превратит главу в пересказ статьи.
- Нужно ли подробнее разбирать review/test? Нет: это граница будущей главы о проверке/evidence.
- Нужно ли подробнее раскрывать persistent work graph? Нет: IV только фиксирует, что `sync` не равен рабочему состоянию.
- Нужно ли сравнивать SPDD с Kiro/spec-kit/TDAD/Constitutional SDD? Нет: это работа главы V.

## Still open after package

- Visual asset-pass: можно ли найти или подготовить локальную схему SPDD-цикла и вставить её после раздела `## Полный цикл в одном изменении`.
- If a figure is added later: она должна показывать жизненный цикл, а не каталог команд или таблицу Canvas.
