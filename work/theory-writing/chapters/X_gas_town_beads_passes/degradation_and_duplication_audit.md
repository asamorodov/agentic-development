# Degradation and duplication audit — Chapter X

## Main risk checks

### Duplication with Chapter VII / PWG

Risk: Beads/PWG lower layer could re-explain persistent work graph. Current P13 keeps it as foundation and quickly moves to flow service. Do not add Beads task graph figure by default.

### Duplication with Chapter VIII / process profiles

Risk: formulas/molecules/gates could become process-profile chapter. Current P13 uses them only to show process becoming persistent work instance. Do not expand.

### Duplication with Chapter IX / runtime

Risk: hooks, Codex lifecycle, `bd prime`, worktrees, permissions could pull chapter into runtime. Current P13 keeps hooks as context-return mechanism. Do not add runtime details.

### Duplication with Chapter XI / evidence

Risk: PR checks, CI, review, merge-ready could become evidence theory. Current P13 explicitly says gates/flow do not prove result quality. Keep this boundary.

### Gas Town atlas duplication

Risk: chapter could become Gas Town overview. P13 mostly avoids this by leading through flow failure and example. P14 notes Gas Town roles are still dense; integrate lifecycle framing.

## Language / style audit

- P13 is much more natural than P10.
- Some English source-native terms remain because they are exact tool terms: `bd prime`, `gt sling`, `hook`, `ready`, `merge failed`, etc.
- Avoid adding more English abstractions like “flow service” in final prose; use “обслуживание потока”.
- Avoid overusing “инфраструктурная потребность” if final pass sees repetition.

## Material preservation

- External source links are now inline for most major facts.
- Direct source link for `gt feed --problems` still needs final integration.
- Figures preserve local assets; no real source image was replaced by a synthetic figure.
