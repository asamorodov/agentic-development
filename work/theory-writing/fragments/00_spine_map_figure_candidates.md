# 00_spine_map_figure_candidates

Статус: визуальный реестр для `00_spine_map.md`.

## Решения для вставки в основной текст

| ID фигуры | Статус | Решение | Проверка полезности / причина |
|---|---|---|---|
| `fig-00-lifecycle-substrate-contract` | `synthetic_figure` | Вставлена в основной фрагмент. | Проходит проверку полезности и нетривиальности: связывает переходы жизненного цикла с носителями и типовыми сбоями; помогает последующим узлам не переопределять термины заново; не подменяет готовое локальное или внешнее изображение. |

## Кандидаты, не вставленные в основной текст

| ID кандидата | Классификация | Решение | Причина отказа / переноса |
|---|---|---|---|
| `fig-00-mode-spectrum` | `editorial_visual_idea` | `deferred_to_A10` | Связь с финальной картой выбора режима добавлена прозой. В 00 отдельная шкала режимов превратила бы терминологический контракт в оглавление будущей теории. |
| `fig-00-theory-stories-atlas-triangle` | `editorial_visual_idea` | `rejected_for_now` | Граница теория / истории / атлас лучше удержана короткой прозой; треугольная схема была бы декоративной и повторяла бы абзацы. |
| `fig-00-pwg-vs-workflow-engine` | `editorial_visual_idea` | `deferred_to_C4_or_atlas` | Различение важно, но 00 должен только закрепить словарь. Подробная схема принадлежит C4 или техническому атласу. |
| `fig-00-evidence-package-card` | `editorial_visual_idea` | `deferred_to_A7_or_fieldbook` | Пакет свидетельств определён в словаре; карточка с полями нужна в A7/Fieldbook, где будет больше фактуры о проверке. |
| `fig-00-technical-atlas-reading-path` | `editorial_visual_idea` | `deferred_to_C5` | Концептуально-технический атлас требует отдельного мостового фрагмента C5, а 00 фиксирует только границу. |
| `fig-00-cross-cutting-checks` | `editorial_visual_idea` | `rejected_for_now` | После усиления 00 появился блок сквозных проверок. Отдельная таблица повторила бы уже вставленную фигуру и превратила бы рабочий контракт в визуальный чеклист. |
| `fig-00-term-load-test-matrix` | `editorial_visual_idea` | `rejected_for_now` | Адресная проверка терминов под нагрузкой оформлена прозой. Отдельная матрица SPDD / PWG / Gas Town / свидетельства / право / ремонт дублировала бы текущую фигуру и сделала бы 00 визуальным чеклистом. |

## Локальные изображения

В разрешённых входах рабочего листа не было каталога ассетов или локальных изображений для 00. Поэтому в основной фрагмент не вставлены элементы `local_image_asset`. Это не означает, что в репозитории нет готовых изображений; это означает, что данный пакет не имел права открывать или проверять их как источниковый вход.

Из `WORKING_DOCUMENTS_MAP.md` известно, что в соседних A/B-фрагментах уже есть готовые локальные изображения и отложенные кандидаты, но 00 не должен автоматически переносить их к себе. Они относятся к A6/A7/A8/A9/B3 или техническому атласу, а не к терминологическому контракту.

## Внешние реальные изображения-кандидаты

После source-pass появились внешние визуальные материалы, но они не вставлены в 00: они относятся к будущим глубоким опорным фрагментам или техническому атласу и требуют отдельного asset-pass и проверки прав.

| ID кандидата | Классификация | Источник | Решение | Причина |
|---|---|---|---|---|
| `fig-00-spdd-reasons-canvas-source-visual` | `external_real_image_candidate` | [Structured-Prompt-Driven Development](https://martinfowler.com/articles/structured-prompt-driven/) | `deferred_to_B1_or_atlas` | В статье есть визуальный материал по REASONS Canvas / SPDD workflow, но 00 не раскрывает SPDD как метод. Нужны asset-pass, проверка прав и решение B1/атласа. |
| `fig-00-gastown-readme-architecture-diagram` | `external_real_image_candidate` | [Gas Town README](https://github.com/gastownhall/gastown) | `deferred_to_B3_or_atlas` | README содержит архитектурную Mermaid-схему Gas Town. Она полезна для B3 или технического атласа, но в 00 превратила бы границу Gas Town / PWG в преждевременный технический разбор. |

Temporal Workflow docs и Nygard ADR article не дали визуального материала, который стоило бы регистрировать для 00.

## Заметки для asset-pass

- `fig-00-lifecycle-substrate-contract` не требует проверки прав как авторская синтетическая фигура.
- Перед публикационной сборкой проверить, не дублирует ли эта фигура сильную композиционную схему из A10 или C5.
- Если позже появится общесайтовая схема жизненного цикла, текущую фигуру можно оставить как краткую терминологическую таблицу или перенести в сопроводительный файл или атлас.


## Адресная проверка терминов под нагрузкой

Визуальный слой основного текста не менялся. Применён asset-classification gate: возможная матрица нагрузочной проверки классифицирована как `editorial_visual_idea` и отклонена для 00, потому что компактный абзац выполняет функцию без дополнительной схемы.


## Проверка против оглавительного тона

Визуальный слой основного текста не менялся. Новые схемы не добавлялись: задача решалась переписыванием одного композиционного абзаца, а не созданием маршрута по будущим частям.


## Общий редакторский проход 1

Визуальных дефектов, требующих правки основного текста, не найдено. Единственная inline-фигура остаётся `synthetic_figure`; новые кандидаты не добавлялись, потому что repair был текстовым и не создавал новой визуальной развилки.


## Общий редакторский проход 2

Визуальных изменений нет. Фигуры не трогались, потому что найденный дефект относился к публичной формулировке границы story/theory, а не к визуальному слою.


## Общий редакторский проход 3

Третий общий проход не выявил визуального дефекта. `fig-00-lifecycle-substrate-contract` остаётся единственной inline-фигурой; отложенные и отклонённые кандидаты не требуют пересмотра на этом шаге.


## Стилевой проход 2

Визуальная проверка пройдена без inline-изменений. Существующая таблица остаётся `synthetic_figure`, внешние реальные изображения SPDD/Gas Town остаются `external_real_image_candidate`, а текстовые идеи остаются отложенными или отклонёнными. Правило «кандидат поддерживает аргумент — значит вставить inline» не применялось.

## Готовность

`ready_with_known_debts`.

Долги:

1. Нужен поздний композиционный проход против A1–A10, чтобы не дублировать их вставленные фигуры.
2. Нужен поздний визуальный проход, если в 00 решат добавить реальное локальное изображение из общего каталога ассетов.
3. Нужно проверить итоговый HTML/CSS для `<figure class="synthetic-figure">` и HTML-таблицы внутри `<figure>`.
