# Отчёт: шаблон плана целевой группы и план A1

Статус: выполнено.

## Что изменено

Обновлён `work/prompts/TARGET_GROUP_PLAN_TEMPLATE.md`: теперь шаблон требует точные списки файлов и готовые финальные тексты prompt-ов, но оставляет prompt-ы короткими рабочими инструкциями без бюрократизации.

Уточнён `work/protocols/TASK_PACKAGE_CREATION_PROTOCOL.md`: фаза подготовки планов целевых групп теперь явно требует готовые prompt-тексты, блок `Прочитай сначала:` в каждом prompt-е и точность, достаточную для сборки пакета.

Создан `work/theory-writing/target-group-plans/A1_CHANGE_NOT_PROMPT_TARGET_GROUP_PLAN.md`: план целевой группы для узла A1 `Единица анализа: программное изменение, а не prompt`.

## План A1

План содержит:

- точный список будущих обрабатываемых файлов;
- полный список read-only материалов;
- готовую очередь из девяти prompt-ов;
- в каждом prompt-е начальный блок `Прочитай сначала:`;
- указание внешних источников как явно заданных групп и выводимых источников из материалов;
- финальную проверку и упаковку результата целевой группы.

## Ограничения

Исполнительный пакет не собран. Создан только build-time plan, по которому пакет можно будет собрать отдельным шагом.
