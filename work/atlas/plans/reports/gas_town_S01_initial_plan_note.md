# S01 initial plan note: `gas_town`

Chosen mode: `create_new`.

Reason:

- Output target `work/atlas/target-group-plans/gas_town_ATLAS_ARTICLE_TARGET_GROUP_PLAN.md` did not exist as a valid target-group plan for explicit worksheet id `gas_town` before this S01 step, so no manual user edits were overwritten.
- The preliminary ledger uses older id `gas_town_beads`; this target plan records normalization to `gas_town` as later ledger-sync debt.
- `work/theory-writing/ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_PLAN.md` is missing from the snapshot, so planning used worksheet, Gas Town dossier, blueprint, ADR-0011, working documents map, discourse, protocols and dossier-backed ledger.

Main planning decisions:

- Treat Gas Town as `core concept / organizational article`: an operating environment for supervised fleets of coding agents.
- Keep Beads central, but rely on the separate PWG article for reusable durable-work-graph mechanism.
- Use full 25-pass atlas queue because Gas Town requires source-depth for Beads, work grammar, roles, scheduler/backpressure, recovery/observability, costs/failures and visual assets.
- Require local image usage for existing Gas Town/Beads assets.
- Preserve costs, sync-risk and human responsibility bottleneck to avoid multi-agent hype.

No article text was written and no article-writing package was built.
