# S01 initial plan note: `bmad_method`

Chosen mode: `create_new`.

Reason:

- Output target `work/atlas/target-group-plans/bmad_method_ATLAS_ARTICLE_TARGET_GROUP_PLAN.md` did not exist as a valid target-group plan for `bmad_method` before this S01 step, so no manual user edits were overwritten.
- The worksheet explicitly asked for article id `bmad_method` while the preliminary ledger still contains older id `bmad`; the plan records this normalization as a later ledger-sync debt.
- `work/theory-writing/ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_PLAN.md` is missing from the snapshot, so planning used the worksheet, BMAD dossier, blueprint, ADR-0011, working documents map, discourse, protocols and dossier-backed ledger.

Main planning decisions:

- Treat BMAD as `method article` and more specifically as artifact-first SDLC process profile for AI-agent development.
- Use full 25-pass atlas queue because the dossier includes phases, files, agents, roles, Quick Flow, Enterprise/TEA, brownfield scan, sprint state, story context, review/repair and many visual candidates.
- Preserve BMAD-specific source detail rather than reducing the article to a smooth summary.
- Make the central boundary BMAD vs PWG/GSD/Gas Town/specification methods/ADR.
- Require visual handling for workflow map, file tree, sprint status, context flow, Quick Flow, brownfield scan, checkpoint/correct-course/investigation/TEA candidates.

No article text was written and no article-writing package was built.
