# S01 note: `gsd_open_gsd`

Mode: `create_new`.

Файл target-group plan не существовал до S01. Создан первичный план для `GSD / Open GSD` как `method article`. План использует full 25-pass blueprint because GSD has gsd-core, gsd-pi, runtime architecture, state, commands, configuration, verification, browser evidence, supervision and failure modes. No article text was written. No article-writing package was built.

Main planning decisions:

- Treat GSD as lifecycle/process runtime for agentic work.
- Preserve distinctions between gsd-core and gsd-pi.
- Use PWG/Gas Town/BMAD/Spec Kit/Kiro as boundary checks.
- Normalize ledger id from `gsd` to `gsd_open_gsd`.
- Keep C5/A10 sync as later debt, not S01 blocker.
