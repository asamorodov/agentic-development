# S01 initial plan note: `persistent_work_graph`

Chosen mode: `create_new`.

Reason:

- Output target `work/atlas/target-group-plans/persistent_work_graph_ATLAS_ARTICLE_TARGET_GROUP_PLAN.md` did not exist as a valid target-group plan for `persistent_work_graph` before this S01 step, so no manual user edits were overwritten.
- `work/theory-writing/ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_PLAN.md` is missing from the snapshot, so planning used the worksheet, PWG dossier, blueprint, ADR-0011, working documents map, discourse, protocols and dossier-backed ledger.

Main planning decisions:

- Treat Persistent Work Graph as `core concept article`, not as Beads product article and not as Gas Town duplicate.
- Use full 25-pass atlas queue because the article must separate durable work state, Beads, issue baselines, Taskmaster, durable execution, source-state/citation protocol, parallel-agent conflict limits and project-design transfer.
- Make the central boundary distinction: work graph answers what work exists and what is ready/waiting; execution graph answers where a run stopped; evidence layer answers why completion is credible.
- Require visual handling for core work graph, ready queue, Beads/Dolt stack, prime recovery, gate-as-wait, job/pass/gate model, source-state flow and safe parallel source protocol.

No article text was written and no article-writing package was built.
