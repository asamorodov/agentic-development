# RESUME — ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_RESULT

## Current state

The target-plan manufactury package completed normally. Ten dossier-backed concept-first atlas article plans were created, repaired and readiness-stamped:

- `persistent_work_graph`
- `gas_town`
- `spdd_method`
- `spec_kit_method`
- `kiro_specs`
- `bmad_method`
- `gsd_open_gsd`
- `adr_method`
- `tdad_comparative`
- `constitutional_sdd`

Every plan has status `ready_for_package_manufacture_after_manual_review`.

## Recommended next work

1. Manually review the target-group plans, especially the article contract, boundary, output files and read-only bundle list.
2. Approve or adjust normalized article ids: `bmad_method`, `tdad_comparative`, `adr_method`, `gas_town`.
3. After C5 exists, sync article tier, atlas placement, reader question, semantic back-links and boundaries.
4. After A10 exists, sync mode-selection / reader-routing implications where relevant.
5. Then manufacture article-writing packages one article at a time, preferably in this order:
   - `persistent_work_graph`
   - `gas_town`
   - `spdd_method`
   - `spec_kit_method` / `kiro_specs`
   - `bmad_method` / `gsd_open_gsd`
   - `adr_method` / `tdad_comparative` / `constitutional_sdd`

## Do not do next by accident

- Do not treat these plans as article drafts.
- Do not skip manual review merely because readiness stamps exist.
- Do not add new atlas article topics unless a dossier or explicit user approval exists.
- Do not replace local image assets with synthetic text figures by default.

## Known debt to carry forward

`work/theory-writing/ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_PLAN.md` is absent from the snapshot. Preserve this as a provenance debt until that file is restored or explicitly superseded.
