# S01 note: `adr_method`

Mode: `create_new`.

Файл target-group plan не существовал до S01. Создан первичный план для `ADR as method / decision memory` как `method article`. План использует full 25-pass blueprint because ADR dossier is dense: classic ADR sources, AI/ADR research, practical agentic examples, executable confirmation, operational checks and many failure modes. No article text was written. No article-writing package was built.

Main planning decisions:

- Treat ADR as decision memory and authority/evidence bridge, not template catalogue.
- Preserve executable/operational confirmation material.
- Use Spec Kit/CSDD/TDAD/PWG as boundary checks.
- Normalize ledger id from `adr` to `adr_method`.
- Keep C5/A10 sync as later debt, not S01 blocker.
