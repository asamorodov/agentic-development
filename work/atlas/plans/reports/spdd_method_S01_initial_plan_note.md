# S01 note: `spdd_method`

Mode: `create_new`.

Reason: `work/atlas/target-group-plans/spdd_method_ATLAS_ARTICLE_TARGET_GROUP_PLAN.md` did not exist before this pass. No existing plan or manual user edits were overwritten.

Main planning decisions:

- Use article id `spdd_method` from the S01 worksheet, not the earlier inferred ledger id `spdd`. Ledger was updated to match the explicit worksheet id.
- Treat SPDD / OpenSPDD as `core concept / method article` because it is both a deep anchor and a concrete method family.
- Keep the full 25-pass blueprint for now. SPDD has enough primary-source depth, OpenSPDD command mechanics, local image assets and boundary risks to justify the full queue.
- Make the future package self-contained: exact read-only paths are listed, including the primary dossier, neighboring dossiers, theory fragments, source usage files, asset catalog files and local SPDD assets.
- Preserve visual materials as assets: Fowler/OpenSPDD-related local images are `local_image_asset` candidates and must not be rewritten as textual diagrams.
- Record shared sync debts: `C5 sync pending`, `A10 sync pending`, and missing `ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_PLAN.md` provenance.

No article text was written. No article-writing package was built.
