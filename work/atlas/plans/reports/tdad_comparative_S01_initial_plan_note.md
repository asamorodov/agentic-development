# S01 note: `tdad_comparative`

Mode: `create_new`.

Файл target-group plan не существовал до S01. Создан первичный план для `TDAD` как `method article`. План использует full 25-pass blueprint because TDAD comparative article has two lines, papers, repos, datasets, benchmark scripts, experiments, skill material, and high risk of collapsing into generic testing overview. No article text was written. No article-writing package was built.

Main planning decisions:

- Treat TDAD as evidence/testing-oriented specification methodology.
- Preserve the internal comparative split between two TDAD lines.
- Use SPDD/Spec Kit/CSDD/ADR/PWG as boundary checks.
- Normalize ledger id from `tdad` to `tdad_comparative`.
- Keep C5/A10 sync as later debt, not S01 blocker.
