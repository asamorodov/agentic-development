# Manufacture report: `tdad_comparative` atlas article target plan

Status: `ready_for_package_manufacture_after_manual_review`.

## S01 mode

S01 used `create_new`: `work/atlas/target-group-plans/tdad_comparative_ATLAS_ARTICLE_TARGET_GROUP_PLAN.md` did not exist. No manual user edits were overwritten.

## Problems found and repaired in S02–S04

### S02

Problems:

- The plan could become generic testing advice or a benchmark inventory.
- It needed explicit justification for TDAD's comparative two-line structure.
- Source/visual policy needed stronger protection for paper tables, benchmark results, datasets and repo screenshots.

Changes:

- Rewrote the queue justification around evidence as upstream definition of agentic work.
- Preserved the two TDAD lines as separate source-depth tracks.
- Added final failure modes: no generic testing overview, no benchmark list without evidence-as-definition mechanism.

### S03

Problems:

- The future article could become testing folklore or benchmark reporting.
- The plan needed an explicit rule distinguishing verification after work from evidence as task definition.

Changes:

- Added `Главный редакторский риск и способ его снять`.
- Added `S03 editorial strengthening` around evidence defining/constraining/interpreting agentic work.

### S04

Problems:

- Source companion updates and visual outputs needed stronger required-output language.
- Real benchmark/paper source objects needed protection from textual replacement.

Changes:

- Strengthened P04–P08 source companion updates.
- Strengthened visual guardrails around paper tables, benchmark results, dataset diagrams and repo screenshots.
- Strengthened P17–P19, P21 and final verification.
- Added `S04 integrated guardrails`.

## Remaining debts

- `work/theory-writing/ATLAS_ARTICLE_TARGET_PLAN_MANUFACTORY_PLAN.md` is missing from the repo snapshot. This weakens provenance of the global known set but does not block this article plan because S01 explicitly specified `tdad_comparative`.
- `C5 sync pending`: after C5/article map exists, sync article tier, boundary, semantic back-links and atlas placement.
- `A10 sync pending`: after A10 exists, sync reader routing and mode-selection placement if relevant.
- Human review should confirm that `method article` is the desired tier and that TDAD's comparative form deserves the full 25-pass queue.

## Package manufacture allowance

Executor package manufacture is allowed after manual review. The plan is mechanically buildable: exact read-only paths were checked in the current snapshot; no missing article-specific read-only file was found.

## Sync pass requirement

Later sync pass required after C5/A10 if those fragments introduce article-map tiers, semantic back-links, or mode-selection decisions that affect this article.
