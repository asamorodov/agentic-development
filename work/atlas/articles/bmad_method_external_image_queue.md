# BMAD Method — external image queue

Статус: P26 завершил guarded final style pass; external queue не изменилась. Внешние изображения не скачивались и не проверялись на права.

## fig-bmad-workflow-map

- Источник: https://docs.bmad-method.org/workflow-map-diagram.html
- Что взять: официальную Workflow Map Diagram BMAD с фазами, ролями, артефактами, Quick Flow и потоком контекста.
- Зачем нужна: главная source diagram для объяснения artifact-first механизма BMAD.
- Предлагаемый локальный путь: `content/assets/atlas-images/bmad-workflow-map-diagram.png`
- Где стоит в тексте: `work/atlas/articles/bmad_method.md`, после раздела «Главная цепочка: от намерения к узкому контексту story».
- Статус: `external-real-candidate`; inline-placeholder сохранён с публичной подписью; перед публикацией нужны скачивание, проверка прав, качества, атрибуции и локализации.
- Текущее решение: внешний кандидат отражён inline и в нижнем разделе «Внешние изображения для asset-pass»; изображение не скачивалось.


## P02 queue sync

P02 introduced new boundary sources but no new external image candidates. Do not add GSD, Beads, Gas Town, Spec Kit, Kiro, SPDD or ADR images to this BMAD article unless a later visual pass proves that a boundary figure is necessary and rights-safe. Current queue remains limited to the official BMAD Workflow Map Diagram.


## P04 queue sync

P04 added no external image candidates. The handoff table is inline text/Markdown, not an asset queue item. Current external queue remains limited to `fig-bmad-workflow-map`.


## P05 queue sync

P05 added no external image candidate. The recovery-surface table is inline Markdown and does not enter the external asset queue.


## P06 queue sync

P06 introduced no external image candidate. Brownfield scan-level material remains inline Markdown; the existing external queue is unchanged.


## P07 queue sync

P07 added no external image candidate. The human-gates decision map is inline Markdown and does not enter the external asset queue.


## P08 queue sync

P08 added no external image candidate. Do not add TEA/release-gate images unless a later visual pass explicitly chooses to represent Enterprise as a bounded extension.

## P09 queue sync

P09 added no external image candidate. Quick Dev material is prose-only and does not require downloading or licensing a new asset. The external queue remains limited to `fig-bmad-workflow-map`.

## P10 queue sync

P10 added no external image candidate. The new reader-path invariant is a prose synthesis and should not expand the external asset queue.

## P11 queue sync

P11 found no local asset that supersedes the external BMAD Workflow Map candidate. The external queue is unchanged and remains limited to `fig-bmad-workflow-map`.

## P12 queue sync

P12 did not add a second external candidate. The official Workflow Map remains the sole external-real asset for this BMAD article. The Getting Started tree is treated as source text/code-like material, and the remaining dossier candidates are synthetic/editorial ideas rather than external images.

## P13 queue sync

P13 was a synthetic-figure pass and did not affect the external image queue. `fig-bmad-workflow-map` remains the only external-real candidate.

## P14 queue sync

P14 added no external image candidate. The audit-report example is illustrative prose and does not affect the external asset queue.

## P15 queue sync

P15 added no external image candidate. The external queue remains unchanged.

## P16 queue sync

P16 added no external image candidate. The theory backlink pass does not affect the asset queue; `fig-bmad-workflow-map` remains the only external-real candidate.

## P17 queue sync

P17 did not add or remove external image candidates. The only queued external-real candidate remains `fig-bmad-workflow-map`; its article-facing description was made more Russian without changing the queue decision.

## P18 — синхронизация очереди изображений

P18 не добавлял и не удалял элементы очереди. Единственный внешний реальный кандидат — `fig-bmad-workflow-map`. В публичной статье теперь по-русски сказано, что перед публикацией нужны скачивание, проверка прав, проверка качества и атрибуция.

## P19 — синхронизация очереди после редакторского прохода

P19 не добавлял внешних изображений и не менял статус Workflow Map. Единственный queued candidate остаётся `fig-bmad-workflow-map` со статусом `external-real-candidate` и долгами по скачиванию, правам, качеству и атрибуции.

## P20 — синхронизация очереди после перестановки разделов

P20 не менял очередь внешних изображений. Перестановка контрактного раздела не затрагивает место `fig-bmad-workflow-map`: кандидат остаётся после раздела «Главная цепочка: от намерения к узкому контексту story».

## P21 — синхронизация очереди после правки заголовков

P21 не менял внешние image candidates. Переходы и заголовки основной статьи не затронули `fig-bmad-workflow-map` и его место в тексте.

## P22 — синхронизация очереди после public-structure pass

Очередь не изменилась: `fig-bmad-workflow-map` остаётся единственным внешним реальным кандидатом. P22 изменил только публичную подпись и нижний статус в статье: без executor note, но с сохранением долга по скачиванию, правам, качеству и атрибуции.

## P23 — external queue companion sync

P23 обновил текущую карточку `fig-bmad-workflow-map`: место в статье, статус inline-placeholder, отсутствие скачивания и будущие проверки указаны без устаревшей P12-формулировки. Новых внешних кандидатов нет.

## P24 queue sync

P24 не добавлял внешних изображений. `fig-bmad-workflow-map` остаётся единственным внешним реальным кандидатом со статусом `external-real-candidate`; долги по скачиванию, правам, качеству, атрибуции и локализации сохраняются.

## P25 queue sync

P25 не добавлял и не удалял внешние изображения. `fig-bmad-workflow-map` остаётся единственным внешним реальным кандидатом; все asset-pass долги сохраняются.

## P26 queue sync

P26 не затронул external image queue. `fig-bmad-workflow-map` остаётся единственным внешним реальным кандидатом; asset-pass долги прежние.
