# Search step 2 results — declarative workflow specifications

## Selected sources

- `AgentSPEX` — agent specification and execution language with explicit control flow, typed steps, branching, loops, reusable submodules, state management, harness, checkpointing, verification and logging.
- Declarative language for LLM-powered workflows — separates workflow specification from implementation and enables multiple backend languages and deployment environments; includes A/B testing and metrics.

## Evaluation

Workflow specification is a different artifact class from feature specification. It describes how agents/tools/states execute work.

## Impact on theory

Add “agent workflow specification” as short/medium artifact in Part VIII or Part VII, not Part V unless comparing to Spec Kit.
