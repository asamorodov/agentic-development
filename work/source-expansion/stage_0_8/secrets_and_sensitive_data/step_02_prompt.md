# Search step 2 — agent skills and credential leakage

Формулировка поиска:

- credential leakage in LLM agent skills large-scale empirical study
- agent skills credentials leakage debug logging stdout exposure LLM agents
- AI coding agent skills secrets credential leaks prompt injection
- MCP configuration credentials exposed secrets AI agents

Цель: проверить whether secrets risk is agent-specific, not just generic software security.
